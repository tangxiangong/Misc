// 函数形参包含多个引用，返回值也含有引用时，需要手动标注生命周期


// 返回引用依赖于两个参数引用，所以在返回值销毁前两个参数不能被销毁，所有两个参数的生命周期均包含返回值的生命周期
fn longer<'a, 'b, 'out>(s1: &'a str, s2: &'b str) -> &'out str
where
    'a: 'out,
    'b: 'out,
{
    if s1.len() > s2.len() {
        s1
    } else {
        s2
    }
}
